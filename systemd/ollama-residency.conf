[Service]
# Two Control Tower chat models, the semantic-cache embedding model, and the
# shared SohamYoga chat model can coexist. Large jobs may still force eviction.
Environment=OLLAMA_MAX_LOADED_MODELS=4
